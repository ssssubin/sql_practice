# Write your MySQL query statement below
select result.teacher_id, count(result.subject_id) as cnt from (select teacher_id, subject_id from Teacher group by teacher_id, subject_id) as result group by teacher_id;